import 'package:flutter/material.dart';

class ScheduleForMe extends StatelessWidget {
  const ScheduleForMe({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
